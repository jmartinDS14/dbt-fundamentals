version: 2

models:
  - name: stg_stripe__payments
    columns: 
      - name: payment_id
        tests:
          - unique
      - name: amount
        tests:
          - assert_dollars_spent_are_greater_than_one:
              group_by_column: order_id